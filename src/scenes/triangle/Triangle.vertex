#version 130

attribute vec2 position;
attribute vec3 color;

varying vec3 passColor;

void main() {
    passColor = color;
    gl_Position.xy = position;
    gl_Position.z = 1.0;
    gl_Position.w = 1.0;
}
